version: '2'
services:
  # db:
  #   image: postgres:11.3
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

  web:
    build: .
    command: bin/rails server --port 3000 --binding 0.0.0.0
    ports:
      - "3000:3000"
#     links:
#       - db
#     volumes:
#       - .:/myapp
# volumes:
#   pgdata:
